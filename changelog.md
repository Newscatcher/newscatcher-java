## 2.1.0 - 2026-03-19
* The SDK now serializes integer-valued doubles without decimal points (e.g., "24000" instead of "24000.0") for cleaner JSON output. This change affects the wire format but maintains full type compatibility.

